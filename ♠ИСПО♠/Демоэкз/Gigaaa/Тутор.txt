Отлично! Раз уж вы нашли на компьютере именно этот вариант (В7) — игрушки, ООО «МирИгрушек» — я подготовлю для вас полное готовое приложение на Python + Tkinter + SQLite, которое полностью соответствует этому заданию. Код будет снабжён подробными комментариями на русском, чтобы вы могли легко разобраться, и отдельными блоками с пояснениями, что менять, если вдруг тема окажется другой (стройматериалы, книги и т.п.). Так как интернета не будет, все необходимые библиотеки уже должны быть установлены (минимум Pillow), а сам код будет самодостаточным.

Ниже я приведу полный код одного файла main.py, который:

Создаёт базу данных database.db в 3НФ со всеми необходимыми таблицами (пользователи, роли, категории, поставщики, производители, единицы измерения, товары, заказы, позиции заказов, пункты выдачи, статусы заказов).

Импортирует данные из файлов, которые вы скинули (Tovar.csv, user_import.csv, Заказ_import.xlsx, Пункты выдачи_import.xlsx), если они лежат в папке import_data рядом с main.py. Если файлов нет — создаёт тестовые данные для демонстрации.

Реализует все модули:

Авторизация (логин/пароль из БД, вход как гость).

Просмотр товаров (фото, название, категория, описание, производитель, поставщик, цена, единица измерения, количество, скидка) с подсветкой (скидка >17% → #FFDEAD, товара нет → голубой, перечёркнутая цена).

Для менеджера и администратора: поиск, фильтрация по поставщику, сортировка по цене и количеству в реальном времени.

Для администратора: добавление, редактирование, удаление товаров (с проверкой, что товар не в заказе), загрузка фото с изменением размера до 300×200, удаление старого фото при замене, запрет на открытие двух окон редактирования.

Для менеджера и администратора: вкладка «Заказы» с просмотром, добавлением, редактированием, удалением заказов (только админ).

Использует единый стиль (шрифт Arial, цвета #FFFFFF, #F5DEB3, #DEB887, #FFDEAD), иконка и логотип (если есть файлы icon.ico и logo.png — подставьте, но код работает и без них).